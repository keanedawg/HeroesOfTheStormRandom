<div class="container">
  <div class="row">
    <div class="offset-1 col-10 my-4">
      <h1>
        Heroes of the Storm Random Hero Selector
      </h1>
    </div>
  </div>
  <div class="row">
    {{#each model as |hero|}}
      <HeroSelectPortrait @hero={{hero.name}} @avatar={{hero.icon_url.92x93}} />
    {{/each}}
  </div>
</div>
{{outlet}}