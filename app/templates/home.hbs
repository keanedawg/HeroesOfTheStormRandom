<div class="container">
    <div class="row">
        {{#each model as |hero|}}
            <HeroSelectPortrait @hero={{hero.name}} @avatar={{hero.icon_url.92x93}}/>
        {{/each}}
    </div>
</div>
{{outlet}}